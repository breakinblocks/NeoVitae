# 1.1.5

## Fixes

- JEI listed only a single ore for meteor entries that roll from a block tag, so the Nether Star meteor looked far poorer than it is. Those slots now cycle through every block the tag can produce, and the tooltip says how many blocks share the estimate. Tag entries pinned to a fixed index now show that block instead of the first one in the tag.
